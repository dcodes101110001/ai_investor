# Git LFS Configuration for AI Investor Repository
# Git LFS is disabled for CSV files because GitHub restricts LFS uploads to public forks
# CSV files in stock_data/ will be committed directly without LFS

# Track other potentially large data files (excluding CSV)
*.parquet filter=lfs diff=lfs merge=lfs -text
*.hdf5 filter=lfs diff=lfs merge=lfs -text
*.h5 filter=lfs diff=lfs merge=lfs -text
